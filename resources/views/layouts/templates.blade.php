@include('layouts.header')

  <div class="section banner_section who_we_help">
  	<div class="container">
  		<h4>@yield('title')</h4>
  	</div>
  </div>

  @yield('content')
  
@include('layouts.footer')
